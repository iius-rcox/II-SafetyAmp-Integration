apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-metrics-scrape
  namespace: safety-amp
spec:
  podSelector:
    matchLabels:
      app: safety-amp
      component: agent
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: kube-system  # adjust to monitoring agents' namespace if different
      ports:
        - protocol: TCP
          port: 9090
